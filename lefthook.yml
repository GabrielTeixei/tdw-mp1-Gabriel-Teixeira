pre-commit:
  parallel: true
  commands:
    lint:
      run: npm run lint
    format:
      run: npm run format
    test:
      run: npm test

pre-push:
  parallel: true
  commands:
    build:
      run: npm run build
